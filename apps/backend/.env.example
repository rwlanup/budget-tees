PORT=4000

# Postgres Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=budget_tees

# Redis Database
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT Config
JWT_ACCESS_SECRET=dev-access-secret-change-me
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL_DAYS=30

# SMTP Config
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=8a5b174da69e81
SMTP_PASSWORD=53b97b19ae1f61
SMTP_FROM_NAME=Budget Tees
SMTP_FROM_ADDRESS=no-reply@budgettees.local

# File Storage Config
STORAGE_DRIVER=local
LOCAL_UPLOAD_DIR=uploads
# S3 Config
S3_BUCKET=
S3_REGION=
S3_ENDPOINT=
S3_ACCESS_KEY=
S3_SECRET_KEY=
CDN_BASE_URL=
PUBLIC_BASE_URL=http://localhost:4000

# Payment Config
# PAYMENT_RETURN_URL = backend base for gateway server-to-server callbacks (must include /api).
PAYMENT_RETURN_URL=http://localhost:4000/api/payments
PAYMENT_WEBSITE_URL=http://localhost:3000
# ESewa ePay v2. ESEWA_SECRET = merchant secret key (HMAC-SHA256 signing key).
# UAT/test values below (product code EPAYTEST); production values are provided by eSewa.
ESEWA_PRODUCT_CODE=EPAYTEST
ESEWA_SECRET=8gBm/:&EnhH.1/q
ESEWA_FORM_URL=https://rc-epay.esewa.com.np/api/epay/main/v2/form
ESEWA_STATUS_URL=https://rc.esewa.com.np/api/epay/transaction/status/