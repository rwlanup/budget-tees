# Keep dependencies that rely on hoisting working under pnpm's strict layout.
shamefully-hoist=false
strict-peer-dependencies=false

# Keep the pnpm content-addressable store inside the project root.
store-dir=.pnpm-store
